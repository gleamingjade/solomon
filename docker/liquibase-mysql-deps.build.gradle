plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

configurations {
    resolvedDeps
}

dependencies {
    resolvedDeps 'com.mysql:mysql-connector-j:8.0.32'
}

tasks.register('copyDeps', Copy) {
    from configurations.resolvedDeps
    into layout.buildDirectory.dir('resolved-libs')
}